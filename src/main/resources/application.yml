server:
  port: 8080

spring:
  application:
    name: ecommerce-Service

  datasource:
    url: jdbc:mysql://localhost:3306/raja
    username: root
    password: rabindra@1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect